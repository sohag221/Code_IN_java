import  java.util.Scanner;
public class Evenet_Duration_Count {
    public static void main(String[] args) {
        //Using C programee;
        //-----------------------------------------------//


       /* int d1,d2,h1,h2,m1,m2,s1,s2,Day,Hourse,Minute,Sec;
        char dial1[5];char del[5];char dial2[5];
        scanf(" %s %d %d %s %d %s %d",dial1,&d1,&h1,del,&m1 ,del,&s1);
        scanf(" %s %d %d %s %d %s %d",dial2,&d2,&h2,del,&m2,del,&s2);

        int ds1=s1+(m1*60)+(h1*60*60)+(d1*24*60*60);
        int ds2=s2+(m2*60)+(h2*60*60)+(d2*24*60*60);

        int totalSec=ds2-ds1;
        Day=totalSec/86400;
        int N=totalSec%86400;
        Hourse=N/3600;
        N=N%3600;
        Minute=N/60;
        Sec=N%60;
        printf("%d dia(s)\n",Day);
        printf("%d hora(s)\n",Hourse);
        printf("%d minuto(s)\n",Minute);
        printf("%d segundo(s)\n",Sec);


        return 0;*/
    }
}
